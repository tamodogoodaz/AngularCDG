import { TelephoneSplitPipe } from './telephone-split.pipe';

describe('TelephoneSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new TelephoneSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
